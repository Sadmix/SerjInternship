/* Примените идеи, которые мы использовали в рrintd, длянаписания рекурсивной версии
функции itoa; иначе говоря, преобразуйте целое число в строку цифр с помощью рекурсивной программы. */

#include <stdio.h>
#include <string.h>

void itoa(int n, char s[]) {
    static int i = 0;  // индекс для вставки символов в строку

    if (n < 0) {  // обрабатываем отрицательное число
        s[i++] = '-';
        n = -n;
    }

    if (n / 10)  // рекурсивный вызов для следующей цифры
        itoa(n / 10, s);

    s[i++] = n % 10 + '0';  // добавляем текущую цифру
    s[i] = '\0';  // завершаем строку
}

int main() {
    int num = -12345;
    char str[100];
    
    itoa(num, str);
    printf("Результат: %s\n", str);
    
    return 0;
}